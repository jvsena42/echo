<journey name="Profile and settings — identity, copy pubky, sign out">
  <description>
    Own profile, the settings screen (identity info, copy pubky) and sign-out back to
    onboarding. Run LAST — it ends the session. Requires a signed-in session.
  </description>
  <actions>
    <action>Launch the Loopky app and verify the Home screen is shown</action>
    <action>Tap the element with resource-id "tab_profile" in the bottom tab bar</action>
    <action>Verify the profile screen shows the user's identity (name or truncated pubky)</action>
    <action>If the account has no display name (the hero falls back to the pubky), verify the "What should we call you?" card is shown under the hero with "Add name" (resource-id: profile_name_nudge_action) and "Not now" (resource-id: profile_name_nudge_dismiss); on a named account verify no such card appears</action>
    <action>If the card is shown, tap "Add name" and verify the Edit Profile sheet opens with an empty name field, then dismiss it</action>
    <action>If the card is shown, tap "Not now" and verify the card disappears; relaunch the app, return to the profile tab and verify it stays gone</action>
    <action>Tap the element with resource-id "profile_settings"</action>
    <action>Verify the Settings screen is shown with an Identity section (truncated pubky, homeserver) and a "Sign out" row (resource-id: settings_signout)</action>
    <action>Tap the element with resource-id "settings_copy_pubky" and verify no crash occurs</action>
    <action>Tap the element with resource-id "settings_signout"</action>
    <action>Verify a confirmation dialog appears</action>
    <action>Confirm sign out</action>
    <action>Verify the app returns to the onboarding screen with the "Sign in with Pubky Ring" button (resource-id: onboarding_signin)</action>
  </actions>
</journey>
